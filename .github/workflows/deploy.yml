name: Deploy VitePress site to Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2 # Versão exata corrigida
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4.2.0 # Versão exata corrigida
        with:
          node-version: 24

      - name: Install dependencies
        run: npm add -D vitepress vue

      - name: Build with VitePress
        run: npx vitepress build docs

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3.1.1 # Versão estável que ignora o bug
        with:
          path: docs/.vitepress/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4.0.5 # Versão exata corrigida
